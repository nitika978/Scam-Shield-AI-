# ScamShield AI

## Run in VS Code

```powershell
python -m venv venv
Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
.\venv\Scripts\Activate.ps1
python -m pip install -r requirements.txt
python -m streamlit run app.py
```

Features: Message Analyzer, URL Analyzer, Live Dashboard, Safety Centre, Low/Moderate/High Risk classification.
